import React from "react"

function NumberOfDogs({dogCount, handleChange}) {
  return (
    <div>
      <input type="text" name="" id="" value={dogCount} onChange={handleChange}/>
    </div>
  )
}

export default NumberOfDogs